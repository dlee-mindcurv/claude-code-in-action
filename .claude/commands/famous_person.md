Use the $ARGUMENT to generate a famous hollywood person and store the name in a variable called:
`$famous_person_name`

Then list the person's name, occupation, and notable works.

Save output to a file called **[$famous_person_name].txt**